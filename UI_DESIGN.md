# U镜 / UCam 界面改版

## 范围

保留 WinForms 与已验证的独立摄像头后端，不引入浏览器壳、不复制 OpenScreen 的功能或标识。参考其浅色工作台结构：紧凑顶栏、大预览区、右侧设置面板。

## 品牌

- 中文：U镜；英文：UCam；新入口：U镜.exe。
- 图标使用维护者提供的原图，保存在 assets/ucam.png，未经重绘。
- build-brand.ps1 仅做保持纵横比的 ICO 格式打包，包含 16/24/32/48/64/128/256 像素规格。
- EXE 和窗口图标、品牌图均嵌入资源，不依赖 Downloads 或临时文件路径。
- Windows 摄像头名称暂保留 Phone USB Camera，CLSID、共享内存名称、注册及协议不变。

## 视觉和操作

- 白色顶栏，浅灰工作区，右侧设备面板；避免原先大标题占据预览空间。
- 橙色主操作 #C1451E，正文 #21252D，辅助文字 #666C77。绿色仅用于真实连接状态。
- Microsoft YaHei UI 字体。外层 14–16 像素圆角，按钮 8–9 像素圆角。
- 等待连接是正常空状态，用灰色；未授权用提示色；不兼容/异常才用红色。
- 镜头列表显示简短名称和 ID；悬停保留完整镜头能力说明。
- 原生下拉控件保留键盘与辅助功能行为，统一文字、选中配色和列表宽度。
- 运行日志只在预览下方展开；主操作在默认/最小尺寸下保持可见。
- 连接中、停止中、断连提示和按钮禁用逻辑继续使用原会话状态。

## 验证边界

- 编译、资源加载、图标、默认/最小窗口、日志展开、模拟连接状态的组件布局检查通过。
- test-output/ucam-ui-*.png 为隐藏控件的合成渲染，不是桌面截图；模拟连接图明确标记了模拟设备。
- 打开新应用的电脑操作授权曾超时，不能据此宣称真实窗口及下拉交互已验证。
- 本轮没有启动手机摄像头、重新注册摄像头，或改动解码/传输核心；前一版本录制链路已由用户确认。
- 新版原生窗口视觉、键盘操作和真机回归仍需用户打开后确认。
