# UCam brand assets

The application artwork was supplied by the project maintainer for UCam.
`ucam.png` preserves the supplied image bytes. `ucam.ico` is generated by
`build-brand.ps1` for Windows application icons, without redrawing the artwork.
The artwork's original authorship and license have not been independently verified.
