# v2.1.1 — 修复 OpenScreen 虚拟摄像头黑屏

修复 v2.1.0 中“主界面有手机画面，但 OBS Virtual Camera / OpenScreen 为纯黑”的问题。

- 手机捕获源不再使用会被 OBS 忽略的工具窗口样式。
- 使用独立隐藏所有者管理任务栏显示，保留集成预览，主界面最小化不应影响后台源。
- 启动时读取虚拟摄像头的实际视频帧；连续纯黑或读取失败会重试并停止会话，避免误报成功。不会保存或上传画面。
- 新增可选真机集成回归检查。

验证：旧版直接读取 OBS Virtual Camera 得到纯黑帧；修复后，Android 16 手机以 3840×2160 / 30fps 采集，应用集成预览窗口管理后读到非纯黑视频帧，测试通过并自动清理采集进程。布局及黑帧检测回归检查通过。

完整解压 `phone-usb-camera-v2.1.1-win64.zip`，先退出旧版，然后运行包内 `无水印手机USB摄像头.exe`。在 OpenScreen 选择 `OBS Virtual Camera`。镜头完全遮挡时可能无法通过启动画面检查，请移开遮挡后重试。
