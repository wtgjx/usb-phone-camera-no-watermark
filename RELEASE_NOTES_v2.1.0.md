# v2.1.0 — 集成预览与简约白色界面

控制面板与手机实时画面合并在一个用户可见窗口中。采用白色卡片、浅灰背景和清晰的主次操作，运行详情默认折叠。

- 后台手机画面源不再显示独立任务栏窗口，保留 scrcpy 和 OBS 的高清输出链路。
- 新增左转 / 右转 90°、水平镜像和预览适应 / 填满控制。
- USB 连接与虚拟摄像头状态自动刷新。
- 修复 Windows 缩放下的布局尺寸，较小窗口中的设置区可滚动。
- 关闭或停止时统一清理本程序启动的采集会话，OpenScreen 保持打开。

下载 `phone-usb-camera-v2.1.0-win64.zip`，完整解压后打开 `无水印手机USB摄像头.exe`。手机需要 Android 12+、USB 调试授权，电脑需要 OBS Studio 和 OpenScreen。在 OpenScreen 中选择 `OBS Virtual Camera`。

验证：集成预览与 OBS Virtual Camera 启动已在本机手机上测试；最终构建通过依赖诊断，以及默认 / 最小窗口 / 展开详情的无前台窗口布局检查。适应 / 填满仅改变主界面预览。可用视频分辨率取决于手机 Camera2 能力。
